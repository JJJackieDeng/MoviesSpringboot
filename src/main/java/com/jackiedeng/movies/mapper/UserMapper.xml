<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jackiedeng.movies.mapper.UserMapper">
    <resultMap type="com.jackiedeng.movies.pojo.User" id="UsersMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userName" column="userName" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="phoneNumber" column="phoneNumber" jdbcType="VARCHAR"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
        <result property="modifiedTime" column="modifiedTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UsersMap">
        select
        id, userName, password, phoneNumber, createTime, modifiedTime
        from movies.users
        where id = #{id}
    </select>

    <!--根据用户名查询单个-->
    <select id="queryByName" resultMap="UsersMap">
        select
        *
        from users
        where
        userName = #{userName}
        and
        password = #{password}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UsersMap">
        select
        id, userName, password, phoneNumber, createTime, modifiedTime
        from movies.users
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UsersMap">
        select
        id, userName, password, phoneNumber, createTime, modifiedTime
        from movies.users
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="username != null and username != ''">
                and userName = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                and phoneNumber = #{phonenumber}
            </if>
            <if test="createtime != null">
                and createTime = #{createtime}
            </if>
            <if test="modifiedtime != null">
                and modifiedTime = #{modifiedtime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into movies.users(userName, password, phoneNumber, createTime, modifiedTime)
        values (#{username}, #{password}, #{phonenumber}, #{createtime}, #{modifiedtime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update movies.users
        <set>
            <if test="username != null and username != ''">
                userName = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                phoneNumber = #{phonenumber},
            </if>
            <if test="createtime != null">
                createTime = #{createtime},
            </if>
            <if test="modifiedtime != null">
                modifiedTime = #{modifiedtime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from movies.users where id = #{id}
    </delete>

</mapper>