<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jackiedeng.movies.mapper.MovieInfoMapper">

    <select id="findAll" parameterType="com.jackiedeng.movies.pojo.MovieInfo" resultType="com.jackiedeng.movies.pojo.MovieInfo">
        select * from movieInfo A left join movie B on A.id=B.id
    </select>
</mapper>